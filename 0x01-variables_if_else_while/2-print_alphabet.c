#include <stdio.h> 

/** 
 *  main - Entry Point 
 *
 *
 *  Return : Always 0 (Success)
 */

int main(void)
{
	char alphabet;

	for (alphabet = 'a' ; alphabet <= 'z' ; alphabet (++))
	{
		putchar(alphabet);
	}
	
		put char('\n'); 

	return(0);
}
